services:
  - type: web
    name: nj-store
    env: python
    plan: free

    buildCommand: pip install -r requirements.txt

    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT

    envVars:
      - key: DB_URL
        scope: run

      - key: ADMIN_PIN
        scope: run
